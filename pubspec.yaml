name: field_sales_mobile
description: "Field Sales Mobile — offline-first Android companion app."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.3

dependencies:
  flutter:
    sdk: flutter
  connectivity_plus: ^6.1.4
  device_info_plus: ^11.3.0
  dio: ^5.9.0
  flutter_secure_storage: ^9.2.4
  intl: ^0.20.2
  path: ^1.9.1
  provider: ^6.1.5
  sqflite: ^2.4.2
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  sqflite_common_ffi: ^2.4.3

flutter:
  uses-material-design: true
